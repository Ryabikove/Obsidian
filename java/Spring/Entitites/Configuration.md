Entity, which stores inside [[Spring/Entitites/Bean|beans]] from classes with specific settings or from foreign libraries.

Creates with annotations:
- [[Spring/Annotations/Entity/Configuration|Configuration]];