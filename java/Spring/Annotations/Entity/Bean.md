Annotation, which allows us to create [[Spring/Entitites/Bean|Bean]] from class. Usually used when it needs to customize creation of Bean or you use a class from a library you didn't write.

Used inside [[Spring/Entitites/Configuration|Configuration]]. 
```
class Bean0 implements Intface

@Configuration
class Configuration {
	@Bean
	public Intface 
	
}
```