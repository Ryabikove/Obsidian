Class, which is used to specify argument type for [[Spring/Testing/Unit tests/Mockito/Mockito|Mockito]] unit tests.

Methods used as detectors in test cases, when you need to detect special data type.

It has several methods which returns value:
- any(Class<T> type) - take class and return its default value.