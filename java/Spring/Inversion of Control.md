[[Spring/Spring|Spring]] takes control of creating objects and handing them out to the classes that need them.

Inversion of control called that way because control inverted from your code to the Spring framework.