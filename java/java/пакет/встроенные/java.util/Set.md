Интерфейс , реализующий неупорядоченную коллекцию уникальных элементов.

Проигнорирует попытку добавить уже существующий элемент.
Скорость проверки на уже существующий элемент О(1) благодаря структуре.
Использует [[java/пакет/класс/обобщения/generics-обобщения|обобщения]] для указания класса, который будет хранить.

#коллекция #интерфейс