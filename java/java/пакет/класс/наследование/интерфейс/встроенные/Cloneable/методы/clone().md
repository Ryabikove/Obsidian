метод интерфейса [[java/пакет/класс/наследование/интерфейс/встроенные/Cloneable/Cloneable|Cloneable]]

обычно (но не по умолчанию) реализация такого метода возвращает вызов метода clone() для родительского класса с преобразованием к типу Person
public Type clone() throws [[CloneNotSupportedException]]{
	return` `(Person)` `super.clone();
}
однако такая реализация осуществляет неполное копирование и подойдет, если клонируемый объект не содержит сложных объектов

#метод