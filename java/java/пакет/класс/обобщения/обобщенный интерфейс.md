специальное объявление интерфейса, при котором тип данных полей или возвращаемых/принимаемых значений определяется при наследовании.

interface Int1\<P\>{
	public void meth(P val);
}
class Test1 implements Int1\<Integer\>;

[[java/пакет/класс/обобщения/generics-обобщения|generics-обобщения]]