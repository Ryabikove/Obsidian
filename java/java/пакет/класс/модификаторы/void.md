указывает, что метод не возвращает никакого значения

==требования==:
 - если в описании метода используется любой другой тип, то метод обязательно должен использовать оператор return

[[пакет/класс/метод/метод|метод]]  [[пакет/класс/метод/return|return]]
#ключевое_слово #модификатор 