метод класса [[пакет/класс/встроенные/Object/Object|Object]]

функционал:
 - принимает объект любого класса и типа
 - возвращает значение типа [[переменные/атрибуты переменных/типы данных/boolean|boolean]].
	int a = 10;
	int b = 12;
	System.out.println(a.equals(b))); // false
#метод