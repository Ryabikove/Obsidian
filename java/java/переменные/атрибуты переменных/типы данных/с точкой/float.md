тип данных переменной

ограничения:
 - по типу данных - числа с плавающей точкой в диапазоне от  -3.4\*10^38 до 3.4\*10^38
 - по объему в памяти - <= 4 байта

[[переменные/атрибуты переменных/типы данных/значения по умолчанию|значения по умолчанию]]
#переменная #тип_данных #число_с_точкой  #ключевое_слово #примитивный_тип