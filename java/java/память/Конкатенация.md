Операция сложения двух строк.
В java при складывании двух строк оператором '+' создается новый объект, а предыдущие 2 остаются.
При многочисленной конкатенации может быть создано большое количество объектов, которые будут занимать место в памяти.