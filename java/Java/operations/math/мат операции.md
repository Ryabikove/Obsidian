большинство операций в Java аналогичны другим си-подобным языкам

присутствуют унарные, бинарные и тренарные операции
 - "+" - сложение двух чисел
 - "-" - вычитание одного числа из другого\
 - "\*" - умножение двух чисел
 - "/" - деление двух чисел
	 если в операции участвуют только целые числа, то результат будет округлен до целого числа, даже если результат присваивается пременной типа [[Java/variables/attributes/data type/primitive/float/Float|Float]] или [[Java/variables/attributes/data type/primitive/float/Double|Double]]
 - "%" - получение остатка от деления двух чисел
 - "++" - инкремент (увеличение значения на 1)
	существуют префиксные и постфиксные инкременты
		в случае префиксного инкремента:
		int a=8;
		int b= ++a;
		System.out.println(a);  // 9
		System.out.println(b);  // 9
		в случае постфиксного инкремента:
		int a=8;
		int b= a++;
		System.out.println(a);  // 9
		System.out.println(b);  // 8
 - "--" - декремент (уменьшение значения на 1)
	то же самое, что и с префиксным и постфиксным декрементом

последовательность операций определяется исходя из приоритета каждого оператора и их ассоциативности

==числа с плавающей точкой при выполнении операций могут давать неточные значения. подобные ошибки возникают из-за того, что на низком уровне для представления применяется двоичная система, однако для числа 0.1 не существует двоичного представления, также как и для других дробных значений. поэтому в таких случаях обычно применяется класс BigDecimal, который позволяет обойти подобные ситуации.==
[[Java/operations/math/приоритет операций|приоритет операций]]  [[Java/operations/math/ассоциативность операций|ассоциативность операций]]

#мат_операции