Built-in class wich used with input streams to collect in ram whole data.

It makes read faster cause store data from input stream for a while.
Without It reading would ask hard drive for a byte every single time, with it reading would be once for whole data.

#class