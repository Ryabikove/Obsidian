[[Java/package/class/наследование/Функциональный интерфейс|Функциональный интерфейс]], представляющий из себя операцию, которая не возвращает результат.

Содержит единственный метод `run()`

```
public interface Runnable {  
    void run();  
}
```

#интерфейс