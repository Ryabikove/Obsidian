Class metadata is information about a [[Java/package/class/Class|class]] that the [[jvm]] stores internally, to understand how the class is structured and how to use it.

It isn't actual [[Java/package/built-in/java/lang/Object/Object|object]] data, but data about the class itself.