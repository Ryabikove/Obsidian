специальное объявление метода, при котором тип данных полей или возвращаемых/принимаемых значений определяется при вызове метода.

public \<A\> void meth(A val){
	val*=2;
	System.out.println(val);
}

this.\<Integer\>meth(val);

[[Java/package/class/обобщения/generics-обобщения|generics-обобщения]]