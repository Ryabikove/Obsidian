специальный метод, содержащийся в каждом классе, который инициализирует объект при его создании

==требования==:

==особенности==:
 - при необходимости выполнять какую-либо логику добавляется вручную, название может быть любым, модификаторы не обязательны
 - если у класса не определен конструктор, при компиляции он создастся автоматически. такой конструктор не принимает и не возвращает никакие параметры.
 - если конструктор не инициализирует значения переменных объекта, то они получают [[Java/variables/attributes/data type/Default value|Default value]]

#метод 