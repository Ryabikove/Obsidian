оператор, используемый в определении метода, указывающий, что тот вызывает исключения непосредственно из блока кода.

синтаксис:
```
public void checkInventory(String[][] shelves) throws DuplicateBookException {}
```

#исключениe #ключевое_слово