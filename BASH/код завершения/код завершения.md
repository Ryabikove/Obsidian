В случае успеха команда, функция внутри сценария или сценарий возвращают 0, в случае ошибки - ненулевое значение, которое интерпретируется как код ошибки (==есть исключения==)

код возврата последней команды хранится в специальной переменной $?

==в отдельных случаях коды возврата должны иметь предопределенные значения и не должны задаваться пользователем==

#код_завершения #функции #командная_строка #переменная 