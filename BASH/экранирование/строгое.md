при таком экранировании любой специальный символ, за исключением \', интерпретируется как простой символ

применяется к строке ограниченной [[одинарные]] кавычками или символу, стоящему после [[обратный]] слеша.

не позволяет выполнять обращение к переменным внутри строк и выполнять команды
#экранирование #кавычки  #подстановка #строки #escape 