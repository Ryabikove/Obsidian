предотвращает интерпретацию [[служебный символ]], за исключением [[знак доллар]] $, \` [[обратные]] кавычки) и \\ escape - [[обратный]] слеш

применяется к строке ограниченной [[двойные]] кавычками

позволяет выполнять обращение к переменным внутри строк и выполнять команды
 echo "today \`date +%T\`, it's $week_day, my name is $(whoami)"

#экранирование #кавычки  #подстановка #строки