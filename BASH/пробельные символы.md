используется как разделитель команд или переменных. В качестве пробельного символа могут выступать:
 - пробел(space)
 - [[символ табуляции]]
 - [[символ перевода строки]]
 - [[символ возврата каретки]]
 - комбинация из вышеперечисленных символов

IFS ([[встроенные переменные]]) переменная, содержащая символы-разделители полей, используемые некоторыми командами. По-умолчанию - пробельные символы.

#пробельные_символы #разделитель_полей #переменная #встроенные_переменные