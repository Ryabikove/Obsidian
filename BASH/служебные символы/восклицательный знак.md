выполняет роль инверсии (логическое отрицание) в [[условные операторы]], инверсируя [[код завершения]] команды, к которой он применен

является [[встроенная команда]]

так же используется для логического отрицания в [[операции сравнения]]
 а != е, ! х > у
 
в некоторых случаях используется для косвенного обращения к переменным ==??==

так же запускает механизм истории командной строки (недоступен из сценариев) ==??== ([[history]])
#переменная #служебный_символ #история #командная_строка #сравнение #встроенная_команда #код_завершения