префикс ключа - с этого символа начинаются опциональные ключи команд
 echo ==-==e
 ==Отдельные внешние и встроенные команды используют символ "+" для разрешения некоторой опции, а символ "-" -- для запрещения.==

перенаправление из/в [[stdin]] в [[stdout]]
 tar cf - . | (cd /dir/; tar xzf -) \# создали архив из файлов в текущем каталоге, в [[subshell]]е перешли в каталог назначения и распаковали архив название которого получили из stdout предыдущей команды tar
 ==в этом контексте "-" - не самостоятельный оператор Bash, а скорее опция, распознаваемая некоторыми утилитами UNIX==

так же одиночный символ "-" перед командами заставит интерпретатор ожидать ввода от пользователя
 \#: cat - \#ждет ввод

в сочетании с командой cd выполняет переход в предыдущий рабочий каталог, путь к которому находится в переменной окружения OLDPWD, так же предыдущий рабочий каталог можно записывать сочетанием с [[тильда]] "~-", будет происходить ссылание на OLDPWD ([[встроенные переменные]])

в [[арифметические операции]] оператор вычитание

#служебный_символ #перенаправление #опции #арифметические_операции #ввод_пользователя #потоки #переменная #встроенные_переменные