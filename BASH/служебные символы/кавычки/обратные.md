используется, для записи подстановка команды в строку, при выводе строки на место команды подставляется её [[вывод]] 
touch \`hostname -c\`-data

таким образом можно задавать [[переменная]]
a=\`date +%T\`

#подстановка #переменная #служебный_символ #кавычки 